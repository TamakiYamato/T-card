using UnityEngine;

public class CardPresenter : MonoBehaviour
{
    /// <summary>
    /// Model‚ÆView‚ğŠÇ—‚·‚éƒNƒ‰ƒXB
    /// </summary>
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
